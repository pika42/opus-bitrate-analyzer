# Temel gereksinimler
numpy>=1.21.0
matplotlib>=3.5.0
tqdm>=4.64.0
pandas>=1.3.0
plotly>=5.5.0
ffmpeg-python==0.2.0

# Ek bağımlılıklar
python-dateutil>=2.8.2
Pillow>=9.0.0  # Matplotlib SVG/PNG kaydetme için
kaleido>=0.2.0  # Plotly statik görsel export için

# FFmpeg/FFprobe sistem gereksinimi (pip ile değil)
# ffmpeg sistemde kurulu olmalı