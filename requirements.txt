# Temel gereksinimler
numpy>=1.21.0
matplotlib>=3.4.0
tqdm>=4.62.0
pandas>=1.3.0
plotly>=5.5.0
ffmpeg-python==0.2.0
kaleido>=0.2.0
python-dateutil>=2.8.2
Pillow>=9.0.0  # Matplotlib for SVG/PNG saving

# FFmpeg/FFprobe system requirement (not with pip)
# ffmpeg must be installed on the system
